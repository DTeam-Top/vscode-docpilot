{
  "timeout": 10000,
  "recursive": true,
  "require": ["ts-node/register"],
  "extension": ["ts"],
  "spec": "src/test/suite/**/*.test.ts",
  "watch-files": ["src/**/*.ts"],
  "watch-ignore": ["node_modules/**/*", "out/**/*"],
  "reporter": "spec",
  "colors": true,
  "bail": false,
  "grep": "",
  "retries": 0
}